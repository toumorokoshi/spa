# spa

YFT's simple single page apps, primarily for personal use.

This repository contains a series of directories, each of which contains a single-page-application that serves a particular purpose.

Although each subdirectory under src is it's own application, the whole project is compiled as as single package and served via github pages.